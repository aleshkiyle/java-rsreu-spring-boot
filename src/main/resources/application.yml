server:
  port:
    8082

spring:
  devtools:
    livereload:
      enabled: true
    restart:
      additional-paths:
        /monitoring
      exclude:
        /monitoring

  data:
    cassandra:
      keyspace-name:
        sanitary_shop
      schema-action:
        recreate
      local-datacenter:
        datacenter1
      username:
        sanitaryShop
      password:
        123

  jpa:
    hibernate:
      ddl-auto:
        update
    show-sql:
      true
    properties:
      hibernate:
        format-sql:
          true